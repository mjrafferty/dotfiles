# vim:ft=bash
################################################################
# Command number (in local history)

typeset -g RIFF_HISTORY_FG="white"
typeset -g RIFF_HISTORY_BG="grey50"

_riff_history_run() {
  _RIFF_RETURN_MESSAGE="%h"
}
