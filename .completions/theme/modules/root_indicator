# vim:ft=bash
################################################################
# Segment to print an icon if user is root.
prompt_root_indicator() {
  "$1_prompt_module" "$0" "$2" "$DEFAULT_COLOR" "yellow" 'ROOT_ICON' 0 '${${(%):-%#}:#%}' ''
}
