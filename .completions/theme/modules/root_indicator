# vim:ft=bash
################################################################
# Segment to print an icon if user is root.

typeset -g RIFF_ROOT_INDICATOR_FG="yellow"
typeset -g RIFF_ROOT_INDICATOR_BG="black"

_riff_root_indicator_run() {
  _RIFF_RETURN_MESSAGE="${${(%):-%#}:#%}"
}
